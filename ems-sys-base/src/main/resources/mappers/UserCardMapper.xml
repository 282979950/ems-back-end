<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tdmh.entity.mapper.UserCardMapper">
    <select id="getUserCardByUserIdAndCardId" parameterType="map" resultType="com.tdmh.entity.UserCard">
        select user_card_id AS userCardId, user_id as userId, card_id as cardId ,card_identifier as cardIdentifier,card_password as cardPassword,
        create_time as  createTime, create_by as createBy,update_time as updateTime,update_by as updateBy ,usable from  user_card
        where user_id = #{userId} and card_id = #{iccardId}
    </select>

    <insert id="insert">
          insert into user_card (user_id, card_id,card_identifier,card_password, create_time, create_by,update_time,update_by ,usable)
          values (#{userId,jdbcType=INTEGER},#{cardId,jdbcType=INTEGER},#{cardIdentifier,jdbcType=VARCHAR},#{cardPassword,jdbcType=VARCHAR},NOW(),#{createBy,jdbcType=INTEGER},NOW(),
          #{updateBy,jdbcType=INTEGER},#{usable,jdbcType=BIT})
    </insert>

    <update id="update">
          update user_card set
          user_id = #{userId},
          card_id = #{cardId},
          card_identifier = #{cardIdentifier},
          card_password = #{cardPassword},
          update_time = NOW(),
          update_by = #{updateBy}
          where user_card_id = #{userCardId}
    </update>
</mapper>